<BrowserRouter>
				<Root>
					<Routes>
						<Route path="/about" element={<About />} />
						<Route path="/login" element={<Login />} />
					</Routes>
				</Root>
			</BrowserRouter>


Todo:

- prop, button title

- the people card: children

- Last : fix card

- fix aside in Root.


COURS:
Use hooks to save state of user:

Se crea un contexto donde en el que ciertas variables
cambian y son accesibles en los componentes children dentro the las tags de autentificacion.

como estilizar tablas, navlinks header text-deco, alinear footer elements...

q: how to maintain user state using hooks, context, make the content match the padding(it exceeds it)

import db from "../db.js"

export function getUserByEmail(email) {
	return db.users.find((user) => user.email === email);
}





ABOUT: useEffect, useParams

useEffect for upload page or values in general, when useStates changes them
useParams recovers a value from path as we have to get the id from the user to know wich profile to display.
